export interface User {
  id: number;
  username: string;
  email: string;
  firstName: string;
  lastName: string;
  gender: string;
  image: string;
  token: string;
  roles?: string[];
}

export interface AccountSchema {
  user: User | null;
}
